{
  "installCommand": "yarn install --ignore-scripts --ignore-engines",
  "buildCommand": "node ./setup && react-scripts build && node ./render",
  "outputDirectory": "build",
  "framework": null,
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
