# Optional: your Dialogflow client token (for local dev the built-in dev token may be expired).
# REACT_APP_DIALOGFLOW_CLIENT_TOKEN=your_token_here

# For chat: use Dialogflow proxy (recommended). Point to your Firebase Functions URL.
# Local emulator (after: firebase use asddsa-rjnq && firebase emulator:start --only functions):
# REACT_APP_API_HOST=http://localhost:5000/asddsa-rjnq/us-central1
